generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/client"
}

datasource db {
  provider     = "mysql"
  relationMode = "prisma"
}

model User {
  id            String    @id @default(uuid())
  email         String    @unique
  emailVerified DateTime? @map("email_verified")
  phone         String    @default("")
  website       String    @default("")

  name     String?
  image    String?
  password String?
  role     ROLE    @default(MANAGER)
  access   ACCESS  @default(USER)

  twoFactorSecret     String?
  tempTwoFactorSecret String?
  twoFactorEnabled    Boolean @default(false)

  createdByAdmin Boolean   @default(false)
  isActive       Boolean   @default(true)
  deactivatedAt  DateTime?

  // Sales-person webhook: receives sales-person events (e.g. lead_assigned)
  webhookUrl String?

  // Stripe
  stripeCustomerId           String?
  stripeSubscriptionId       String?
  stripePaymentMethodId      String?
  stripePriceId              String?
  plan                       PLAN?
  stripeSubscriptionStatus   String?
  stripeSubscriptionCancelAt DateTime?
  stripeCurrentPeriodEnd     DateTime?
  stripeSubscriptionTerm     PLAN_TERM?
  planIsActive               Boolean    @default(false)

  //razorpay
  razorpayCustomerId           String?    @unique
  razorpaySubscriptionEndDate  DateTime?
  razorpaySubscriptionCancelAt DateTime?
  razorpayPlanPriceId          String?
  razorpayPlanPeriod           PLAN_TERM?
  razorpaySubscriptionId       String?    @unique
  razorpaySubscriptionStatus   String?

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations

  agencyId      String?
  Agency        User?   @relation("AgencyToMembers", fields: [agencyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  AgencyMembers User[]  @relation("AgencyToMembers")

  // sales agent (business relation)
  businessId String?
  Business   Business? @relation("BusinessSalesAgents", fields: [businessId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  ownerBusinessId String?
  OwnerBusiness   Business? @relation("BusinessOwner", fields: [ownerBusinessId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  Config Config?

  Account         Account[]
  Session         Session[]
  AgencyLeads     Lead[]    @relation("AgencyToLead")
  SalesAgentLeads Lead[]    @relation("SalesAgentToLead")

  SalesAgentMeetings Meeting[]  @relation("SalesAgentToMeeting")
  AgencyMeetings     Meeting[]  @relation("AgencyToMeeting")
  AgencyCalls        LeadCall[] @relation("AgencyToLeadCall")
  AgentCalls         LeadCall[] @relation("SalesAgentToLeadCall")

  agencyApiKeys  ApiKey[] @relation("AgencyToApiKey")
  managerApiKeys ApiKey[] @relation("ManagerToApiKey")

  Campaigns            Campaign[]
  CampaignLeads        CampaignLead[]
  NumberPools          NumberPool[]
  PoolPhoneNumbers     PoolPhoneNumber[]
  Assistant            Assistant[]
  Pipelines            Pipeline[]
  Opportunities        Opportunity[]
  Stages               Stage[]
  Events               Event[]
  AgentFollowUps       FollowUp[]            @relation("SalesAgentToFollowUp")
  AgencyFollowUps      FollowUp[]            @relation("AgencyToFollowUp")
  ConversationMessages ConversationMessage[]
  Conversations        Conversation[]
  BaseAgentSessions    BaseAgentSession[]
  AgencyBusinesses     Business[]            @relation("BusinessAgency")
  ManagerBusinesses    BusinessManager[]
  preferenceId         String?
  Agents               Agent[]
  PrimaryAgents        PrimaryAgent[]
  ChatSessions         ChatSession[]
  VoiceSessions        VoiceSession[]
  Automations          Automation[]
  Knowledgebases       Knowledgebase[]
  BaseAgentsAsAgency   BaseAgent[]

  @@index([agencyId])
  @@index([businessId])
  @@index([ownerBusinessId])
}

model ApiKey {
  id        String    @id @default(uuid())
  name      String?
  isActive  Boolean   @default(true)
  createdAt DateTime  @default(now())
  lastUsed  DateTime?

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation("AgencyToApiKey", fields: [agencyId], references: [id], onDelete: Cascade)

  managerId String?
  Manager   User?   @relation("ManagerToApiKey", fields: [managerId], references: [id], onDelete: Cascade)

  @@index([agencyId])
  @@index([businessId])
  @@index([managerId])
}

model Assistant {
  id String @id @default(uuid())

  name        String
  description String @db.Text
  config      Json?

  // Relations
  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  Campaigns Campaign[]

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([agencyId])
  @@index([businessId])
}

model Config {
  id String @id @default(uuid())

  // White Label Configuration
  whiteLabel               Boolean @default(false)
  showCRM                  Boolean @default(true)
  showCampaign             Boolean @default(true)
  customDomain             String?
  customRootDomain         String?
  customRootDomainVerified Boolean @default(false)
  companyName              String  @default("")
  onlyLogo                 Boolean @default(false)
  companyLogo              String  @default("")
  primaryColor             String  @default("#000000")
  secondaryColor           String  @default("#ffffff")
  faviconUrl               String  @default("")
  title                    String  @default("")
  description              String? @db.Text

  // Nameserver-based wildcard domain support
  isWildcard            Boolean @default(false)
  wildcardSetupMethod   String? // 'nameservers' or 'dns'
  nameserversConfigured Boolean @default(false)

  // Database Configuration
  databaseUrl     String?
  migrationStatus MIGRATION_STATUS @default(PENDING)
  migratedAt      DateTime?

  // Relations 
  agencyId String @unique
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

/// Primary-DB registry: maps external webhook ids (IG user id, WA phone_number_id, …)
/// to the agency whose tenant DB holds the integration data. No secrets duplicated.
model WebhookRoute {
  id         String   @id @default(uuid())
  provider   String
  externalId String
  agencyId   String
  businessId String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([provider, externalId])
  @@index([agencyId])
}

model BusinessConfig {
  id String @id @default(uuid())

  // White Label Configuration
  whiteLabel               Boolean @default(false)
  subdomain                String? // For agency subdomain (e.g., business.agency.com)
  customDomain             String?
  customRootDomain         String?
  customRootDomainVerified Boolean @default(false)
  companyName              String  @default("")
  onlyLogo                 Boolean @default(false)
  companyLogo              String  @default("")
  primaryColor             String  @default("#000000")
  secondaryColor           String  @default("#ffffff")
  faviconUrl               String  @default("")
  title                    String  @default("")
  description              String? @db.Text

  // Limitations
  maxLeads Int @default(100)

  // Relations 
  businessId String   @unique
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([subdomain])
}

model Account {
  id                String  @id @default(uuid())
  type              String
  provider          String
  providerAccountId String  @map("provider_account_id")
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  userId String @map("user_id")
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
  @@map("accounts")
}

model Session {
  id           String   @id @default(uuid())
  sessionToken String   @unique @map("session_token")
  userId       String   @map("user_id")
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("sessions")
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

model Lead {
  id String @id @default(uuid())

  // personal information
  name           String
  firstName      String    @default("")
  lastName       String    @default("")
  email          String?
  bestEmail      String    @default("")
  emailVerified  Boolean   @default(false)
  phone          String?
  bestPhone      String    @default("")
  phoneVerified  Boolean   @default(false)
  dateOfBirth    DateTime?
  leadIsVerified Boolean   @default(false) // if either email or phone is verified
  profileImage   String?

  // general info
  ianaTimezone String  @default("") // Asia/Kolkata
  country      String  @default("") // country of the lead
  city         String  @default("") // city of the lead
  state        String  @default("") // state of the lead
  zipCode      String  @default("") // zip code of the lead
  address      String? @db.Text // address of the lead

  // additional info 
  company         String  @default("") // company name of the lead
  companyShortName String @default("") // company short name of the lead

  companyAddress String? @db.Text
  industry       String  @default("") // ex: Real Estate, Marketing etc.
  website        String  @default("")
  message        String? @db.Text // message sent by the lead
  channel        CHANNEL @default(EMAIL) // lead source channel
  note           String? @db.Text // note about the lead by agency/agent
  fields         Json? // custom fields

  customData Json? // custom data (variables)

  // DND (Do Not Disturb)
  emailDND    Boolean @default(false)
  smsDND      Boolean @default(false)
  callDND     Boolean @default(false)
  whatsappDND Boolean @default(false)

  // Channel Availability - which channels are active/available for this contact
  emailActive     Boolean @default(false)
  smsActive       Boolean @default(false)
  whatsappActive  Boolean @default(false)
  facebookActive  Boolean @default(false)
  instagramActive Boolean @default(false)
  liveChatActive  Boolean @default(false)
  voiceCallActive Boolean @default(false)

  // Channel Specific Metadata Fields
  instagramSID     String?
  instaUsername    String?
  facebookSenderId String?
  facebookUsername String?

  // Metadata 
  status     LEAD_STATUS      @default(NEW)
  statusType LEAD_STATUS_TYPE @default(COLD)
  quality    LEAD_QUALITY     @default(UNQUALIFIED)

  /// Where the lead was first created (e.g. Insta OS automation vs other flows).
  leadSource LEAD_SOURCE @default(UNKNOWN)

  /// First contact sub-source for Insta OS (comment vs DM). Set only on create; see LEAD_SUB_SOURCE.
  leadSubSource LEAD_SUB_SOURCE @default(UNKNOWN)

  /// MiniApp billing period end for the last DM-agent assignment slot this lead consumed (matches MiniAppSubscription.nextLimitResetAt at that time). Null until first counted; new period allows another slot.
  instaOsDmAgentQuotaPeriodEndAt DateTime?

  // other fields
  ghlContactId String?

  /// CRMBridge PMS patient id (opaque foreign key — not a local patient record).
  crmPatientId String?
  /// CRMBridge appointment id after booking writeback.
  crmAppointmentId String?

  /// Stable browser id from website chat embed (localStorage on host page). Unique per business.
  embedVisitorId String?

  // Relations
  agencyId String
  Agency   User   @relation("AgencyToLead", fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  salesAgentId String?
  SalesAgent   User?   @relation("SalesAgentToLead", fields: [salesAgentId], references: [id], onDelete: SetNull)

  isTestLead     Boolean  @default(false)
  // Metadata Fields
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  Meeting              Meeting[]
  LeadCalls            LeadCall[]
  CampaignLead         CampaignLead[]
  LeadTags             LeadTag[]
  LeadNotes            LeadNote[]
  Opportunity          Opportunity[]
  FollowUp             FollowUp[]
  Conversation         Conversation?
  ConversationMessages ConversationMessage[]
  ChatSessions         ChatSession[]
  VoiceSessions        VoiceSession[]
  BaseAgentSessions    BaseAgentSession[]
  AutomationSessions   AutomationSession[]
  CustomFieldValue     CustomFieldValue[]
  ChatLogs             ChatLog[]
  InstaOsEvents         InstaOsEvent[]
  InstaOsDmOutboxes     InstaOsDmOutbox[]
  LeadJourneyEvents     LeadJourneyEvent[]
  LeadIgAccountConfigs  LeadIgAccountConfig[]
  DentalBridgeBookings  DentalBridgeBooking[]
  DentalBridgePatient   DentalBridgePatient?
  GmbReviews            GmbReview[]
  FunnelFormSubmissions FunnelFormSubmission[]

  @@index([agencyId])
  @@index([salesAgentId])
  @@index([businessId])
  @@index([status])
  @@index([phone])
  @@index([email])
  @@index([ghlContactId])
  @@index([businessId, leadSource])
  @@index([businessId, leadSubSource])
  @@index([businessId, crmPatientId])
  @@unique([businessId, embedVisitorId])
}

model LeadTag {
  id String @id @default(uuid())

  name  String
  color String

  // Relations
  Leads Lead[]

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([name, businessId])
  @@index([businessId])
}

model LeadNote {
  id String @id @default(uuid())

  note String         @db.Text
  type LEAD_NOTE_TYPE @default(NOTE)

  // Relations
  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([leadId])
}

/// Append-only lead journey (global; not Insta-OS–only). Written only via createLeadJourneyEvent().
model LeadJourneyEvent {
  id         String            @id @default(uuid())
  text       String            @db.Text
  actor      LEAD_JOURNEY_ACTOR
  metadata   Json?

  leadId     String
  Lead       Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt  DateTime @default(now())

  @@index([leadId, createdAt])
  @@index([businessId, leadId])
}

enum LEAD_JOURNEY_ACTOR {
  SYSTEM
  USER
  AUTOMATION
}

model Conversation {
  id String @id @default(uuid())

  starred Boolean @default(false)
  killed  Boolean @default(false)

  replyMode REPLY_MODE @default(AUTO)

  // Follow-up configuration
  followUpActive      Boolean @default(false)
  activeFollowUpIndex Int     @default(0)

  // Relations
  leadId String @unique
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  activeAgentId String?
  ActiveAgent   PrimaryAgent? @relation(fields: [activeAgentId], references: [id], onDelete: SetNull)

  /// Insta OS Base Agent handling DM chat for this thread (separate from Recall PrimaryAgent).
  activeBaseAgentId String?
  ActiveBaseAgent   BaseAgent? @relation("ConversationActiveBaseAgent", fields: [activeBaseAgentId], references: [id], onDelete: SetNull)

  /// Instagram account this DM thread belongs to (token + sender IGSID for Graph).
  instagramAccountId String?
  InstagramAccount   InstagramAccount? @relation(fields: [instagramAccountId], references: [id], onDelete: SetNull)

  /// WhatsApp account this thread belongs to (Cloud API phone_number_id).
  whatsAppAccountId String?
  WhatsAppAccount   WhatsAppAccount? @relation(fields: [whatsAppAccountId], references: [id], onDelete: SetNull)

  /// WhatsApp free-form messaging service window. Reset by the lead's inbound
  /// WhatsApp message, intentionally buffered before Meta's 24h cutoff.
  whatsAppServiceWindowExpiresAt DateTime?

  /// Instagram standard-messaging service window. Reset by the lead's inbound
  /// IG DM, intentionally buffered before Meta's 24h cutoff.
  instagramServiceWindowExpiresAt DateTime?

  // Metadata
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  lastMessageAt DateTime @default(now())

  // Relationships
  ConversationMessages ConversationMessage[]
  BaseAgentSessions    BaseAgentSession[]

  @@index([leadId])
  @@index([agencyId])
  @@index([businessId])
  @@index([activeAgentId])
  @@index([activeBaseAgentId])
  @@index([instagramAccountId])
  @@index([id, createdAt])
  @@index([whatsAppAccountId])
}

model ConversationMessage {
  id String @id @default(uuid())

  content String                      @db.Text
  sender  MESSAGE_SENDER              @default(AI_AGENT)
  status  CONVERSATION_MESSAGE_STATUS @default(SENT)
  snippet String                      @default("")

  channel  CHANNEL           @default(EMAIL)
  itemType MESSAGE_ITEM_TYPE @default(MESSAGE)
  source   MESSAGE_SOURCE    @default(RECALLSYNC)

  // SYSTEM EVENTS (tool_calls etc)
  systemEvent       SYSTEM_EVENT?
  systemEventStatus SYSTEM_EVENT_STATUS?
  systemData        Json?
  systemDescription String?              @db.Text // ex: Appointment booked for 10:00 AM on 10th July 2025

  ghlMessageId String?
  /// Platform message id for native channels (Instagram `mid`, WhatsApp `wamid`).
  /// Stored for both inbound and outbound messages so quoted replies can be
  /// resolved back to the local message row.
  externalMessageId String? @db.VarChar(191)
  /// Raw platform message id this inbound message replied to, when supplied by
  /// Instagram / WhatsApp.
  replyToExternalId String? @db.VarChar(191)
  /// Snapshot of the quoted message content. This keeps model history cheap and
  /// preserves context even if the referenced message is later edited/deleted.
  replyToContent String? @db.Text
  replyToSender  MESSAGE_SENDER?

  /// Set for LEAD inbound from GHL recall-high webhook; @@unique skips duplicate webhook/worker retries.
  sourceInboundDedupKey String? @db.VarChar(512)

  rsRunId String?

  // Call Message Items
  callId String?
  Call   LeadCall? @relation(fields: [callId], references: [id], onDelete: Cascade)

  // Relations
  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  conversationId String
  Conversation   Conversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)

  agentId String?
  Agent   Agent?  @relation(fields: [agentId], references: [id], onDelete: Cascade)

  /// IG account that sent/received this message (channel INSTAGRAM only).
  instagramAccountId String?
  InstagramAccount   InstagramAccount? @relation(fields: [instagramAccountId], references: [id], onDelete: SetNull)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  /// AES-encrypted chat body fields when Dental Bridge HIPAA mode is enabled.
  isPhiEncrypted Boolean @default(false)
  /// When set, cron purges this row after the retention window (creation + 14 days).
  phiDeleteAt    DateTime?

  @@index([leadId])
  @@index([conversationId])
  @@index([conversationId, instagramAccountId, createdAt])
  @@index([instagramAccountId])
  @@index([externalMessageId])
  @@index([conversationId, businessId, externalMessageId])
  @@index([callId])
  @@index([agencyId])
  @@index([businessId])
  @@index([agentId])
  @@index([rsRunId])
  @@index([phiDeleteAt])
  @@unique([sourceInboundDedupKey])
}

/// One row per claimed GHL InboundMessage delivery (dedup across webhook/QStash/worker retries).
model GhlInboundWebhookDelivery {
  dedupKey            String    @id @db.VarChar(512)
  claimedAt           DateTime  @default(now())
  enqueuedAt          DateTime?
  processingStartedAt DateTime?
  completedAt         DateTime?
  lastError           String?   @db.Text
  payloadJson         String    @db.Text

  @@index([completedAt])
  @@index([claimedAt])
}

enum PRIMARY_AGENT_TYPE {
  // Agency-level primary, selectable in the Recall agent picker.
  RECALL
  // Per-business auto-managed primary owned by the IgThreadly mini-app.
  // One per business; created on demand when the first STANDARD/FLOW
  // BaseAgent is created inside IgThreadly. Not selectable in the Recall picker.
  IGTHREADLY
}

model PrimaryAgent {
  id String @id @default(uuid())

  type PRIMARY_AGENT_TYPE @default(RECALL)

  name         String
  internalName String  @default("")
  description  String? @db.Text

  agentGoal               String? @db.Text
  goalCompleteCriteria    String? @db.Text
  stopScenarioDescription String? @db.Text

  // agent training 
  // {url}/train -> agent traning url 
  // {url}/create -> create vector indexes 
  // {url}/update -> update vector indexes
  // {url}/delete -> delete vector indexes
  n8nTrainingUrl   String @default("")
  n8nTrainingToken String @default("")

  // e.g. { "enabled": true, "triggers": [{ "type": "RESTART_SESSION", "value": "/restart" }] }
  actionTriggers Json?

  // Relations
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  ghlCalendarId         String?
  calenderIntegrationId String?
  calendarType          CALENDAR_TYPE        @default(CAL)
  CalenderIntegration   CalenderIntegration? @relation(fields: [calenderIntegrationId], references: [id], onDelete: SetNull)

  // nextPrimaryAgent
  nextPrimaryAgentId String?
  NextPrimaryAgent   PrimaryAgent?  @relation("NextPrimaryAgents", fields: [nextPrimaryAgentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  NextPrimaryAgents  PrimaryAgent[] @relation("NextPrimaryAgents")

  Agents             Agent[]
  Conversations      Conversation[]
  PrimaryAgentAssets PrimaryAgentAsset[]
  ChatSessions       ChatSession[]
  VoiceSessions      VoiceSession[]
  Campaigns          Campaign[]
  AutomationSessions AutomationSession[]
  Automations        Automation[]
  Knowledgebases     Knowledgebase[]
  BaseAgents  BaseAgent[] @relation("PrimaryAgentBaseAgents")

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([businessId, type])
  @@index([agencyId])
  @@index([calenderIntegrationId])
  @@index([nextPrimaryAgentId])
}

model PrimaryAgentAsset {
  id String @id @default(uuid())

  name        String  @default("")
  description String? @db.Text

  // UPLOADTHING
  fileUrl  String
  fileKey  String
  fileType String
  fileSize Int    @default(0) // in KB

  // Relations
  primaryAgentId String
  PrimaryAgent   PrimaryAgent @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([primaryAgentId])
}

model Agent {
  id String @id @default(uuid())

  name        String
  description String? @db.Text

  // n8n config 
  n8nUrl        String  @default("")
  n8nToken      String  @default("")
  // n8n workflow id
  n8nWorkflowId String?

  // agent settings
  settings Json
  isActive Boolean    @default(false)
  agentMode AGENT_MODE @default(AUTO)
  provider PROVIDER?

  // sequence config
  sequenceActive Boolean @default(false)
  sequenceConfig Json?   @default("{}") // sequence config json 

  // followup config 
  followupActive Boolean @default(false)
  followupConfig Json?   @default("{}") // followup config json 

  channel CHANNEL    @default(EMAIL)
  type    AGENT_TYPE @default(INTEGRATED)

  // integrated channel config 
  flow           Json?            @default("{\"nodes\":[{\"id\":\"start-1\",\"type\":\"start\",\"position\":{\"x\":100,\"y\":200},\"data\":{\"label\":\"Start\",\"description\":\"Conversation starts here\"}}],\"edges\":[]}") // react flow json 
  flowSettings   Json?            @default("{}") // react flow settings json 
  /// Draft builder state (Save draft); Publish copies into flow/flowSettings
  currentFlow         Json? @default("{\"nodes\":[{\"id\":\"start-1\",\"type\":\"start\",\"position\":{\"x\":100,\"y\":200},\"data\":{\"label\":\"Start\",\"description\":\"Conversation starts here\"}}],\"edges\":[]}") // react flow draft json
  currentFlowSettings Json?
  voiceAgentType VOICE_AGENT_TYPE @default(CONVERSATION_PATHWAY)

  // VAPI
  vapiPhoneNumberId String?
  vapiAssistantId   String?

  //ELEVEN_LABS
  elevenlabsAgentId       String?
  elevenlabsPhoneNumberId String?

  // RETELL
  retellAgentId     String?
  retellPhoneNumber String?

  //TWILIO
  twilioPhoneNumber String?
  // Relations
  businessId        String
  Business          Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  primaryAgentId String
  PrimaryAgent   PrimaryAgent  @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)
  ChatSessions   ChatSession[]
  VoiceSessions  VoiceSession[]

  useN8N       Boolean @default(false)
  // Smart Trigger - auto-create / chat session without a dedicated agent trigger
  smartTrigger Boolean @default(false)

  /// Auto-attach primary agent and run on inbound DM (WhatsApp / Instagram direct channels).
  autoConnect Boolean @default(false)

  /// Native Instagram: scoped inbox when provider is INSTAGRAM (required). Null for GHL/N8N.
  instagramAccountId String?
  InstagramAccount   InstagramAccount? @relation("InstagramAccountWorkflowAgents", fields: [instagramAccountId], references: [id], onDelete: SetNull)

  /// Native WhatsApp / WP_VOICE_CALL: scoped line when provider is WHATSAPP or channel WP_VOICE_CALL (required). Null for GHL/N8N.
  whatsAppAccountId String?
  WhatsAppAccount   WhatsAppAccount? @relation("WhatsAppAccountWorkflowAgents", fields: [whatsAppAccountId], references: [id], onDelete: SetNull)

  // Structured outputs for VOICE_CALL agents (all providers)
  structuredOutputs Json? @default("[]")

  // Field mapping for GHL custom fields
  fieldMapping Json? @default("[]")

  /// Website voice embed: { UI: { title, description, startButtonLabel, endButtonLabel }, ... }
  widgetSettings Json?

  // Metadata
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  AgentTools           AgentTool[]
  ConversationMessages ConversationMessage[]

  @@index([businessId])
  @@index([agencyId])
  @@index([primaryAgentId])
  @@index([createdAt])
  @@index([businessId, autoConnect, channel])
  @@index([instagramAccountId])
  @@index([whatsAppAccountId])
}

model AgentTool {
  id String @id @default(uuid())

  definition Json

  Agents      Agent[]
  BaseAgents  BaseAgent[]

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
}

model Meeting {
  id        String @id @default(uuid())
  meetingId String @unique

  startTime     String
  messageOfLead String         @db.Text
  meetingUrl    String?
  status        MEETING_STATUS @default(UPCOMING)

  // Relations
  salesAgentId String?
  SalesAgent   User?   @relation("SalesAgentToMeeting", fields: [salesAgentId], references: [id], onDelete: SetNull)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation("AgencyToMeeting", fields: [agencyId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  calendarType  CALENDAR_TYPE  @default(CAL)
  meetingSource MEETING_SOURCE @default(PLATFORM)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([agencyId])
  @@index([salesAgentId])
  @@index([businessId])
  @@index([leadId])
}

model LeadCall {
  id         String  @id @default(uuid())
  callId     String  @default("")
  notes      String? @db.Text
  summary    String? @db.Text

  structuredOutput Json? @default("{}")

  transcript Json?
  audioUrl   String  @default("")

  type          CALL_TYPE            @default(OUTBOUND)
  endedReason   CALL_END_REASON      @default(unknown_error)
  callStat      CALL_STAT            @default(NOT_PICKED)
  userReplyStat CALL_USER_REPLY_STAT @default(NO_ANSWER)
  result        CALL_RESULT          @default(SUCCESS)
  callType      LEAD_CALL_TYPE       @default(GLOBAL)
  callSource    CALL_SOURCE          @default(VAPI)

  callDuration  Float @default(0)
  costVoice     Float @default(0) // total voiceAi cost
  costTelephony Float @default(0) // total telephony cost
  totalCost     Float @default(0) // total call cost

  // Relations
  leadId String?
  Lead   Lead?   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation("AgencyToLeadCall", fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  salesAgentId String?
  SalesAgent   User?   @relation("SalesAgentToLeadCall", fields: [salesAgentId], references: [id], onDelete: SetNull)

  campaignId String?
  Campaign   Campaign? @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  poolNumberId    String?
  PoolPhoneNumber PoolPhoneNumber? @relation(fields: [poolNumberId], references: [id], onDelete: SetNull)

  // Metadata
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  ConversationMessages ConversationMessage[]
  VoiceSessions      VoiceSession[]

  @@index([agencyId])
  @@index([salesAgentId])
  @@index([leadId])
  @@index([campaignId])
  @@index([businessId])
  @@index([poolNumberId])
}

model Campaign {
  id String @id @default(uuid())

  name        String
  description String          @db.Text
  status      CAMPAIGN_STATUS @default(DRAFT)

  // Settings
  withRetries       Boolean   @default(false)
  withFollowUp      Boolean   @default(false)
  retries           Int       @default(3)
  maxRetryAttempts  Int       @default(3)
  callBackDelay     Int       @default(60)
  concurrentCalls   Int       @default(1)
  startDate         DateTime?
  startTime         String    @default("05:30")
  workingHoursStart String    @default("09:00")
  workingHoursEnd   String    @default("17:00")
  timeZone          String    @default("IST")
  settingsUpdated   Boolean   @default(false)

  // New Settings Fields
  retryInterval     Int?    @default(2)
  retryIntervalType String? @default("hour") // "hour" | "day"
  weeklySchedule    Json? // Weekly schedule with time slots per day

  // Channels the campaign's primary agent should contact on (passed to triggerAgent).
  // JSON array of CHANNEL strings + NONE for no channels. Empty = all of the agent's channels.
  channels Json?

  /** Max new outbound lead pickups per local day (voice + SMS + WhatsApp). null = unlimited. */
  dailyOutboundLimit Int?
  /** Running count for today; reset at midnight in campaign.timeZone. */
  outboundToday      Int @default(0)

  // Relations
  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  assistants Assistant[]

  primaryAgentId String?
  PrimaryAgent   PrimaryAgent? @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)

  // Optional outbound automation (cadence) run alongside this campaign.
  automationId String?
  Automation   Automation? @relation(fields: [automationId], references: [id], onDelete: SetNull)

  // Optional outbound caller-ID pool for voice rotation.
  numberPoolId String?
  NumberPool   NumberPool? @relation(fields: [numberPoolId], references: [id], onDelete: SetNull)

  type              CAMPAIGN_TYPE @default(VOICE_REACTIVATION)
  gmbReviewSettings Json?

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  CampaignLeads CampaignLead[]
  LeadCalls     LeadCall[]
  GmbReviews    GmbReview[]

  @@index([agencyId])
  @@index([businessId])
  @@index([primaryAgentId])
  @@index([automationId])
  @@index([numberPoolId])
  @@index([businessId, type, status])
}

model NumberPool {
  id   String @id @default(uuid())
  name String

  defaultDailyCap        Int @default(50)
  defaultConcurrentLimit Int @default(2)
  defaultMinGapSeconds   Int @default(8)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  numbers   PoolPhoneNumber[]
  campaigns Campaign[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([agencyId])
  @@index([businessId])
}

model PoolPhoneNumber {
  id String @id @default(uuid())

  e164              String
  provider          VOICE_POOL_PROVIDER
  providerId        String
  Provider          Provider @relation(fields: [providerId], references: [id], onDelete: Cascade)
  providerNumberRef String

  status            POOL_NUMBER_STATUS @default(WARMING)
  dailyCap          Int
  concurrentLimit   Int
  minGapSeconds     Int

  callsToday      Int       @default(0)
  concurrentCalls Int       @default(0)
  lastCallAt      DateTime?

  acceptanceRate Float?
  pausedReason   String? @db.Text

  poolId     String
  NumberPool NumberPool @relation(fields: [poolId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  leadCalls LeadCall[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([poolId, e164])
  @@index([poolId])
  @@index([providerId])
  @@index([agencyId])
  @@index([businessId])
  @@index([status])
}

model CampaignLead {
  id String @id @default(uuid())

  campaignId String
  Campaign   Campaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  status     CAMPAIGN_LEAD_STATUS  @default(PENDING)
  quality    CAMPAIGN_LEAD_QUALITY @default(LOW)
  result     CAMPAIGN_LEAD_RESULT  @default(SUCCESS)
  notes      String?               @db.Text
  followUpAt DateTime?

  calledAt    DateTime?
  retriesLeft Int       @default(3)
  nextRetryAt DateTime?

  GmbReviews GmbReview[]

  @@index([agencyId])
  @@index([businessId])
  @@index([campaignId])
  @@index([leadId])
}

model Pipeline {
  id   String @id @default(uuid())
  name String

  // Relations
  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  stages        Stage[]
  Opportunities Opportunity[]

  @@index([agencyId])
  @@index([businessId])
}

model Stage {
  id    String @id @default(uuid())
  name  String
  order Int    @default(0)

  Pipeline   Pipeline @relation(fields: [pipelineId], references: [id], onDelete: Cascade)
  pipelineId String

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  Opportunities Opportunity[]

  @@index([pipelineId])
  @@index([agencyId])
  @@index([businessId])
}

model Opportunity {
  id           String             @id @default(uuid())
  name         String
  description  String?            @db.Text
  status       OPPORTUNITY_STATUS @default(OPEN)
  source       String             @default("")
  value        Float?
  businessName String?

  order Int @default(0)

  // Relations
  stageId String
  Stage   Stage  @relation(fields: [stageId], references: [id], onDelete: Cascade)

  pipelineId String
  Pipeline   Pipeline @relation(fields: [pipelineId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  addedToStageAt DateTime @default(now())

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([stageId])
  @@index([leadId])
  @@index([agencyId])
  @@index([pipelineId])
  @@index([businessId])
}

model Preference {
  id String @id @default(uuid())

  businessId String   @unique
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  leadPreference Json?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
}

// Automation
model Event {
  id String @id @default(uuid())

  name        String
  description String  @db.Text
  url         String  @db.Text
  token       String
  isActive    Boolean @default(false)

  event EVENT @default(OPPORTUNITY_EVENT)

  // Relations
  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, event])
  @@index([agencyId])
  @@index([businessId])
}

// Follow up
model FollowUp {
  id String @id @default(uuid())

  reason     String           @db.Text // why follow up
  notes      String           @db.Text // notes
  status     FOLLOW_UP_STATUS @default(PENDING)
  summary    String           @db.Text // conversation summary
  followUpAt DateTime // when to follow up
  priority   PRIORITY         @default(LOW) // priority of the follow up
  attempts   Int              @default(0) // number of attempts
  type       FOLLOW_UP_TYPE   @default(AI_AGENT) // type of follow up
  source     FOLLOW_UP_SOURCE @default(MANUAL_SCHEDULED) // source of the follow up
  channel    CHANNEL          @default(EMAIL)

  referenceId String? // for meeting followup reference id will be meeting id

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  salesAgentId String?
  SalesAgent   User?   @relation("SalesAgentToFollowUp", fields: [salesAgentId], references: [id], onDelete: SetNull)

  agencyId String
  Agency   User   @relation("AgencyToFollowUp", fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([agencyId])
  @@index([salesAgentId])
  @@index([leadId])
  @@index([createdAt])
  @@index([businessId])
  @@index([referenceId])
}

model Business {
  id      String  @id @default(uuid())
  name    String
  image   String  @default("")
  email   String
  phone   String
  address String? @db.Text

  // bizz razorpay
  planIsActive                 Boolean    @default(false)
  plan                         PLAN       @default(FREE)
  razorpayCustomerId           String?    @unique
  razorpaySubscriptionEndDate  DateTime?
  razorpaySubscriptionCancelAt DateTime?
  razorpayPlanPriceId          String?
  razorpayPlanPeriod           PLAN_TERM?
  razorpaySubscriptionId       String?    @unique
  razorpaySubscriptionStatus   String?

  // Twilio
  twilioAccountSid String @default("")

  // VAPI
  vapiPrivateKey String @default("")

  // Trieve
  trieveApiKey         String @default("")
  trieveOrganizationId String @default("")

  // n8n
  n8nWebhookSecret String @unique @default(uuid())
  n8nWebhookUrl    String @default("")

  setupCompleted Boolean @default(false)

  // Onboarding progress tracking
  onboardingProgress Json? // Stores onboarding step completion status

  // Mini-app source tracking
  miniAppSource MINI_APP_SOURCE?

  /// Hard cap on total Lead rows for this business (anti-abuse, not plan-based).
  /// Enforced at lead creation across every source (Recall, API, mini-apps).
  /// Configurable per business; default 100k. Lead creation is skipped/blocked at/over this.
  leadHardCap Int @default(100000)

  // Relations
  agencyId String
  Agency   User   @relation("BusinessAgency", fields: [agencyId], references: [id], onDelete: Cascade)

  // New relation for managers
  SalesAgents User[] @relation("BusinessSalesAgents")

  // Business Owners 
  BusinessOwners User[] @relation("BusinessOwner")

  // Rebilling fields
  lastMessagesChargedAt DateTime @default(now())
  lastVoiceChargedAt    DateTime @default(now())
  chargeConfig          Json? // prices set by agency for messages and voice

  // Metadata Fields
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  BusinessIntegration  BusinessIntegration?
  BusinessManagers     BusinessManager[]
  ApiKeys              ApiKey[]
  Assistants           Assistant[]
  Leads                Lead[]
  LeadTag              LeadTag[]
  Conversation         Conversation[]
  ConversationMessage  ConversationMessage[]
  Meetings             Meeting[]
  Campaigns            Campaign[]
  CampaignLeads        CampaignLead[]
  Pipelines            Pipeline[]
  Stages               Stage[]
  Opportunities        Opportunity[]
  Preference           Preference?
  Events               Event[]
  FollowUps            FollowUp[]
  LeadCalls            LeadCall[]
  BusinessConfig       BusinessConfig?
  Agents               Agent[]
  PrimaryAgents        PrimaryAgent[]
  calenderIntegrations CalenderIntegration[]
  n8nIntegrations      N8NIntegration[]
  ChatSessions         ChatSession[]
  VoiceSessions        VoiceSession[]
  Providers            Provider[]
  VoiceChargeLogs      VoiceChargeLog[]
  MessageChargeLogs    MessageChargeLog[]
  Automations          Automation[]
  CustomField          CustomField[]
  AgentTools           AgentTool[]
  SipTrunks            SipTrunk[]
  Knowledgebases       Knowledgebase[]
  InstagramAccounts    InstagramAccount[]
  WhatsAppAccounts     WhatsAppAccount[]
  WhatsAppMessageTemplates WhatsAppMessageTemplate[]
  InstaOsAutomation    InstaOsAutomation[]
  DmTemplates          DmTemplate[]
  InstaOsEvents         InstaOsEvent[]
  InstaOsDmOutboxes     InstaOsDmOutbox[]
  LeadJourneyEvents     LeadJourneyEvent[]
  LeadIgAccountConfigs  LeadIgAccountConfig[]
  BaseAgents     BaseAgent[] @relation("BusinessBaseAgents")
  BaseAgentSessions     BaseAgentSession[]
  MiniAppSubscriptions  MiniAppSubscription[]
  NumberPools           NumberPool[]
  PoolPhoneNumbers      PoolPhoneNumber[]
  DentalBridgeSettings  DentalBridgeSettings?
  DentalBridgeBookings  DentalBridgeBooking[]
  DentalBridgePatients  DentalBridgePatient[]
  LocalPresenceSettings LocalPresenceSettings?
  BusinessMailConnection BusinessMailConnection?
  BusinessKeywords      BusinessKeyword[]
  KeywordRankingSyncs   KeywordRankingSync[]
  GmbReviews            GmbReview[]
  LandingPages          LandingPage[]
  Websites              Website[]
  FunnelForms           FunnelForm[]
  BusinessAssets        BusinessAsset[]

  @@index([agencyId])
}

/// Dental Bridge (DentWire) per-business settings — CRMBridge defaults, booking mode, HIPAA.
model DentalBridgeSettings {
  id         String   @id @default(uuid())
  businessId String   @unique
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  hipaaMode   Boolean                  @default(false)
  bookingMode DENTAL_BRIDGE_BOOKING_MODE @default(DIRECT)

  /// CRMBridge clinic identifiers (credentials live on Provider CRM_BRIDGE).
  crmPracticeId              String @default("")
  defaultProviderId          String @default("")
  defaultOperatoryId         String @default("")
  defaultAppointmentReason   String @default("New Patient Exam")
  defaultAppointmentLengthMinutes Int @default(60)
  slotSearchDaysAhead        Int    @default(14)

  /// Stripe deposit path (DFY manual key entry per business). Nullable — MySQL TEXT cannot use @default("").
  stripeSecretKey      String? @db.Text
  stripeWebhookSecret  String? @db.Text
  stripeDepositAmountCents Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
}

/// Shared Local Presence settings (GMB review URL, SEO defaults).
model LocalPresenceSettings {
  id         String   @id @default(uuid())
  businessId String   @unique
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  googleReviewUrl      String? @db.Text
  targetDomain         String?
  defaultLocationCode  Int?
  defaultLocationName  String?
  seoTimezone          String  @default("UTC")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
}

/// Multi-page website container — bound to a custom domain when connected.
model Website {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  name   String
  slug   String
  /// Public hostname e.g. www.acmedental.com — globally unique when set.
  hostname           String? @unique
  hostnameVerified   Boolean @default(false)
  settings           Json?

  folders WebsiteFolder[]
  pages   LandingPage[]
  globalComponents WebsiteGlobalComponent[]

  /// Assigned site-wide header/footer global component ids (no FK — avoids relation cycles).
  headerGlobalComponentId String?
  footerGlobalComponentId String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, slug])
  @@index([businessId])
}

/// Nested folder within a website — slug segments appear in public URLs.
model WebsiteFolder {
  id        String  @id @default(uuid())
  websiteId String
  Website   Website @relation(fields: [websiteId], references: [id], onDelete: Cascade)
  parentId  String?
  Parent    WebsiteFolder?  @relation("WebsiteFolderTree", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  Children  WebsiteFolder[] @relation("WebsiteFolderTree")

  name      String
  slug      String
  sortOrder Int    @default(0)

  pages LandingPage[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([websiteId, parentId])
  @@index([websiteId])
}

enum WEBSITE_GLOBAL_COMPONENT_KIND {
  HEADER
  FOOTER
  CTA
  CUSTOM
}

/// Reusable section scoped to a website — resolved live on all pages.
model WebsiteGlobalComponent {
  id        String   @id @default(uuid())
  websiteId String
  Website   Website  @relation(fields: [websiteId], references: [id], onDelete: Cascade)

  name    String
  kind    WEBSITE_GLOBAL_COMPONENT_KIND @default(CUSTOM)
  /// Full SectionNode JSON (section → rows → columns → elements)
  section Json

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([websiteId])
}

/// AI-built landing pages (draft/publish, block JSON).
model LandingPage {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  websiteId String
  Website   Website @relation(fields: [websiteId], references: [id], onDelete: Cascade)
  folderId  String?
  Folder    WebsiteFolder? @relation(fields: [folderId], references: [id], onDelete: SetNull, onUpdate: NoAction)

  title  String
  slug   String
  status LANDING_PAGE_STATUS @default(DRAFT)

  /// Homepage for this website — serves `/` on the connected domain.
  isHomePage    Boolean @default(false)
  /// Canonical published path e.g. `/`, `/about`, `/services/service-1`
  publishedPath String?

  blocks Json
  theme  Json

  /// Live snapshot — updated only on publish; public /p/[id] reads this.
  publishedBlocks Json?
  publishedTitle  String?
  publishedSlug   String?
  publishedMetaTitle       String?
  publishedMetaDescription String? @db.Text

  metaTitle       String?
  metaDescription String? @db.Text

  publishedAt DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([websiteId, folderId])
  @@index([websiteId, isHomePage])
  @@index([websiteId, publishedPath])
  @@index([businessId, status])
  @@index([status])
}

/// Business-scoped media library (UploadThing-backed).
model BusinessAsset {
  id         String            @id @default(uuid())
  businessId String
  Business   Business          @relation(fields: [businessId], references: [id], onDelete: Cascade)

  name             String
  originalFileName String
  fileUrl          String
  fileKey          String
  mimeType         String
  type             BusinessAssetType
  fileSizeBytes    Int               @default(0)

  uploadedByUserId String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId, type, createdAt])
  @@index([businessId])
}

/// Reusable funnel form definitions (fields, webhooks, submissions).
model FunnelForm {
  id                 String   @id @default(uuid())
  businessId         String
  Business           Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  name               String
  isActive           Boolean  @default(true)
  externalWebhookUrl String?  @db.Text
  webhookToken       String   @unique

  fields      FunnelFormField[]
  submissions FunnelFormSubmission[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, name])
  @@index([businessId])
}

model FunnelFormField {
  id     String     @id @default(uuid())
  formId String
  Form   FunnelForm @relation(fields: [formId], references: [id], onDelete: Cascade)

  key         String
  label       String
  type        FUNNEL_FORM_FIELD_TYPE
  required    Boolean @default(false)
  placeholder String?
  sortOrder   Int     @default(0)

  @@unique([formId, key])
  @@index([formId])
}

model FunnelFormSubmission {
  id         String     @id @default(uuid())
  formId     String
  Form       FunnelForm @relation(fields: [formId], references: [id], onDelete: Cascade)
  businessId String

  landingPageId String?
  data          Json
  leadId        String?
  Lead          Lead?   @relation(fields: [leadId], references: [id], onDelete: SetNull)

  webhookStatus FUNNEL_WEBHOOK_STATUS @default(PENDING)
  webhookError  String?               @db.Text

  createdAt DateTime @default(now())

  @@index([formId, createdAt])
  @@index([businessId, createdAt])
}

/// Per-business Gmail or Outlook mailbox for review request emails.
model BusinessMailConnection {
  id         String               @id @default(uuid())
  businessId String               @unique
  Business   Business             @relation(fields: [businessId], references: [id], onDelete: Cascade)

  provider       BusinessMailProvider
  accessToken    String?   @db.Text
  refreshToken   String?   @db.Text
  tokenExpiresAt DateTime?
  accountEmail   String?
  accountId      String?
  connectedAt    DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
}

/// GMB review request / collection (Lead-scoped).
model GmbReview {
  id         String       @id @default(uuid())
  businessId String
  Business   Business     @relation(fields: [businessId], references: [id], onDelete: Cascade)
  leadId     String
  Lead       Lead         @relation(fields: [leadId], references: [id], onDelete: Cascade)

  campaignId     String?
  Campaign       Campaign?     @relation(fields: [campaignId], references: [id], onDelete: SetNull)
  campaignLeadId String?
  CampaignLead   CampaignLead? @relation(fields: [campaignLeadId], references: [id], onDelete: SetNull)

  status          ReviewStatus @default(QUEUED)
  rating          Int?
  content         String?      @db.Text
  requestedAt     DateTime?
  requestReason   String?      @db.Text
  requestCount    Int          @default(0)
  failureCount    Int          @default(0)
  pendingSubject  String?
  pendingBodyHtml String?      @db.Text
  respondedAt     DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId, status, createdAt])
  @@index([leadId])
  @@index([campaignId])
}

model BusinessKeyword {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  keyword      String
  targetDomain String
  locationCode Int
  locationName String

  rankings BusinessKeywordRanking[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, keyword, locationCode])
  @@index([businessId])
}

model BusinessKeywordRanking {
  id        String          @id @default(uuid())
  keywordId String
  keyword   BusinessKeyword @relation(fields: [keywordId], references: [id], onDelete: Cascade)
  syncId    String?
  sync      KeywordRankingSync? @relation(fields: [syncId], references: [id], onDelete: SetNull)

  source       RankingSource @default(GOOGLE_ORGANIC)
  provider     String        @default("dataforseo")
  position     Int?
  rankingUrl   String?       @map("ranking_url") @db.Text
  rankingTitle String?
  topDomains   Json?

  checkedAt DateTime
  checkedOn DateTime @db.Date
  device    String   @default("desktop")
  error     String?  @db.Text

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([keywordId, source, checkedOn])
  @@index([keywordId, checkedAt])
  @@index([syncId])
}

model KeywordRankingSync {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  source       RankingSource @default(GOOGLE_ORGANIC)
  provider     String        @default("dataforseo")
  startedAt    DateTime
  finishedAt   DateTime?
  totalCount   Int
  successCount Int           @default(0)
  failedCount  Int           @default(0)
  skippedCount Int           @default(0)

  rankings BusinessKeywordRanking[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId, startedAt])
}

/// Async CRMBridge patient writeback — optional 1:1 with Lead.
model DentalBridgePatient {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  leadId     String   @unique
  Lead       Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)

  status DENTAL_BRIDGE_PATIENT_STATUS @default(QUEUED)

  firstName String
  lastName  String
  email     String?
  phone     String?

  crmPatientId   String?
  /// CRMBridge async writeback id from CreateNewPatient.
  crmWritebackId String?
  bridgeMessage  String? @db.Text
  bridgeStatus   String?
  writebackError String? @db.Text

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([businessId, status])
  @@index([crmWritebackId])
}

/// Pending booking hold (Stripe deposit path) before PMS writeback.
model DentalBridgeBooking {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  leadId     String
  Lead       Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)

  status DENTAL_BRIDGE_BOOKING_STATUS @default(PENDING_PAYMENT)

  appointmentDate     String
  timeHour            Int
  timeMin             Int
  providerId          String
  operatoryId         String
  appointmentReason   String
  appointmentLengthMinutes Int @default(60)

  stripeInvoiceId String?

  crmPatientId      String?
  crmAppointmentId  String?
  /// CRMBridge async writeback id from CreateNewAppointment (poll via GetWritebackStatus cron).
  crmWritebackId    String?
  /// Latest Message from CRMBridge queue/status responses.
  bridgeMessage     String? @db.Text
  /// Latest Status from CRMBridge GetWritebackStatus (e.g. InProgress, Completed, Failed).
  bridgeStatus      String?
  writebackError    String? @db.Text

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId, leadId])
  @@index([businessId, status])
  @@index([stripeInvoiceId])
  @@index([crmWritebackId])
}

/// Per–mini-app billing and usage (Recall core subscription stays on `Business`).
model MiniAppSubscription {
  id         String   @id @default(uuid())
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  miniAppSource MINI_APP_SOURCE

  plan         MINI_APP_SUBSCRIPTION_PLAN @default(FREE)
  planIsActive Boolean @default(true)

  /// Distinct leads that received an automated Instagram DM this billing period (automation + base agent). Lead rows are unlimited.
  newLeadsThisPeriod Int @default(0)
  nextLimitResetAt   DateTime?

  razorpaySubscriptionId       String?    @unique
  razorpayPlanPriceId          String?
  razorpayPlanPeriod           PLAN_TERM?
  razorpaySubscriptionEndDate  DateTime?
  razorpaySubscriptionCancelAt DateTime?
  razorpaySubscriptionStatus   String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, miniAppSource])
  @@index([businessId])
}

model InstagramAccount {
  id              String    @id @default(uuid())
  /// Graph API IG User id from GET /me → `id`. Use in paths like `/{instagramUserId}/messages`.
  instagramUserId String    @unique
  /// From GET /me → `user_id` when it differs from `id`. Matches webhook `entry.id` / DM `recipient.id`.
  instagramProfessionalId String? @unique
  username        String
  name            String    @default("")
  profilePicture  String?   @db.Text
  accessToken     String    @db.Text
  tokenExpiresAt  DateTime?
  isConnected     Boolean   @default(true)
  /// Within plan slot: webhooks, automations, and outbound use this account. Others stay linked but paused.
  isProcessingEnabled Boolean @default(true)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  InstaOsAutomations InstaOsAutomation[]
  Conversations      Conversation[]
  ConversationMessages ConversationMessage[]
  LeadIgAccountConfigs LeadIgAccountConfig[]
  BaseAgentsScoped      BaseAgent[] @relation("InstagramAccountBaseAgents")
  WorkflowAgentsScoped  Agent[]     @relation("InstagramAccountWorkflowAgents")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([businessId, createdAt])
}

/// Per-(lead, Instagram account) inbox state: service window and sort key for scoped inbox.
model LeadIgAccountConfig {
  id                 String @id @default(uuid())
  leadId             String
  businessId         String
  instagramAccountId String

  instagramServiceWindowExpiresAt DateTime?
  lastMessageAt                   DateTime  @default(now())

  Lead             Lead             @relation(fields: [leadId], references: [id], onDelete: Cascade)
  Business         Business         @relation(fields: [businessId], references: [id], onDelete: Cascade)
  InstagramAccount InstagramAccount @relation(fields: [instagramAccountId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([leadId, instagramAccountId])
  @@index([businessId, instagramAccountId, lastMessageAt])
  @@index([instagramAccountId])
  @@index([leadId])
}

model WhatsAppAccount {
  id                 String    @id @default(uuid())
  businessId         String
  Business           Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  agencyId           String

  metaBusinessId     String
  wabaId             String
  phoneNumberId      String    @unique
  displayPhoneNumber String
  verifiedName       String    @default("")

  accessToken        String    @db.Text
  tokenExpiresAt     DateTime?

  webhookSubscribed  Boolean   @default(false)
  /// When set, inbound Meta webhook payloads are forwarded here (raw body + headers).
  forwardWebhookUrl  String?
  isConnected        Boolean   @default(true)
  connectedAt        DateTime  @default(now())
  disconnectedAt     DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  BaseAgents         BaseAgent[] @relation("WhatsAppAccountBaseAgents")
  WorkflowAgents     Agent[]     @relation("WhatsAppAccountWorkflowAgents")
  Conversations      Conversation[]
  MessageTemplates   WhatsAppMessageTemplate[]

  @@unique([businessId, phoneNumberId])
  @@index([businessId])
  @@index([wabaId])
}

enum WHATSAPP_TEMPLATE_STATUS {
  DRAFT
  PENDING
  APPROVED
  REJECTED
  PAUSED
}

model WhatsAppMessageTemplate {
  id                String                   @id @default(uuid())
  businessId        String
  Business          Business                 @relation(fields: [businessId], references: [id], onDelete: Cascade)
  whatsAppAccountId String
  WhatsAppAccount   WhatsAppAccount          @relation(fields: [whatsAppAccountId], references: [id], onDelete: Cascade)

  name            String
  language        String                   @default("en_US")
  category        String?
  status          WHATSAPP_TEMPLATE_STATUS @default(PENDING)

  headerType     String?
  headerContent  String?
  headerMediaUrl String?
  bodyText       String                   @db.Text
  footerText     String?
  buttons        Json?
  sampleValues   Json?

  metaTemplateId  String?
  rejectionReason String?
  qualityScore    String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, whatsAppAccountId, name, language])
  @@index([businessId, status])
  @@index([whatsAppAccountId])
}

// ─── Insta OS Models ────────────────────────────────────────────────────────

model InstaOsAutomation {
  id         String           @id @default(uuid())
  businessId String
  instagramAccountId String
  name       String           @default("Instagram Comment Automation")
  isActive   Boolean          @default(false)
  postScope  InstaOsPostScope @default(ALL_POSTS)
  version    Int              @default(1)

  Business Business            @relation(fields: [businessId], references: [id], onDelete: Cascade)
  InstagramAccount InstagramAccount @relation(fields: [instagramAccountId], references: [id], onDelete: Cascade)

  nodes         InstaOsNode[]
  instaOsEvents  InstaOsEvent[]
  instaOsDmOutboxes InstaOsDmOutbox[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([instagramAccountId])
  @@index([businessId, instagramAccountId])
}

/// Paced outbound: DM and/or public comment reply share one QStash flow key per sender IG (`ig.{id}`) — one Graph call per job (Meta ~200/h combined for messages + comment replies).
model InstaOsDmOutbox {
  id     String @id @default(uuid())
  businessId   String
  automationId String
  leadId       String
  /// Instagram comment / trigger id (for sendInstagramDm recipient.comment_id)
  commentId    String
  /// Webhook `parent_id` for thread replies; `POST /replies` must target a top-level id (use this for Graph, not `commentId`).
  publicReplyTargetCommentId String?
  /// Sending business IGSID — use for QStash FlowControl key `ig.{senderInstagramUserId}` (Meta pool is per account).
  senderInstagramUserId String
  /// Rendered DM body; null if automation only posts a public reply.
  messageText  String?  @db.Text
  /// Public comment reply text; null if DM-only.
  replyText     String?  @db.Text
  /// True after `POST /messages` succeeds (or when there is no DM to send).
  dmDelivered  Boolean  @default(false)
  /// True after `POST /{commentId}/replies` succeeds (or when there is no public reply to post).
  commentReplyDelivered Boolean @default(false)
  templateId   String?
  templateName String?
  routingPath  String?
  mediaId         String?
  commentText     String? @db.Text
  senderIgUserId  String?
  senderUsername  String?
  instaOsEventId  String  @unique

  status       InstaOsDmOutboxStatus @default(PENDING)
  attemptCount Int     @default(0)
  lastError    String? @db.Text
  nextRetryAt  DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  Business   Business         @relation(fields: [businessId], references: [id], onDelete: Cascade)
  Lead       Lead             @relation(fields: [leadId], references: [id], onDelete: Cascade)
  automation InstaOsAutomation @relation(fields: [automationId], references: [id], onDelete: Cascade)
  instaOsEvent InstaOsEvent  @relation(fields: [instaOsEventId], references: [id], onDelete: Cascade)

  @@unique([automationId, commentId])
  @@index([status, nextRetryAt])
  @@index([senderInstagramUserId, status])
  @@index([businessId])
  @@index([leadId])
}

model InstaOsNode {
  id           String          @id @default(uuid())
  automationId String
  type         InstaOsNodeType
  sortOrder    Int
  enabled      Boolean         @default(true)
  config       Json

  automation InstaOsAutomation @relation(fields: [automationId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([automationId, type])
  @@index([automationId])
  @@index([automationId, sortOrder])
}

/// Per-channel: OFF = no message; STATIC = body / replyVariations; AI = LLM (classifier path only; keyword is always static).
enum DmTemplateChannel {
  OFF
  STATIC
  AI
}

model DmTemplate {
  id               String  @id @default(uuid())
  businessId       String
  name             String
  slug             String
  body             String  @db.Text
  // 3–10 comment reply variations when publicChannel=STATIC, validated at application layer.
  replyVariations  Json?
  isPublic         Boolean @default(false)
  category         String?
  tags             Json?
  dmChannel     DmTemplateChannel @default(STATIC)
  publicChannel DmTemplateChannel @default(STATIC)
  aiCommentInstructions String?        @db.Text
  aiDmInstructions      String?        @db.Text

  Business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([businessId, slug])
  @@index([businessId])
}

// ─── Insta OS Enums ──────────────────────────────────────────────────────────

model InstaOsEvent {
  id             String             @id @default(uuid())
  businessId     String
  automationId   String
  leadId         String
  commentId      String             // Instagram comment ID (used for dedup key)
  mediaId        String?            // Post/reel that received the comment
  senderIgUserId String?            // Who commented
  senderUsername String?
  commentText    String             @db.Text
  routingPath    String?            // KEYWORD_MATCH | AI_MATCH | AI_NO_MATCH | NO_RULES
  templateId     String?
  templateName   String?
  status         InstaOsEventStatus @default(PENDING)
  errorMessage   String?            @db.Text
  createdAt      DateTime           @default(now())

  Business   Business            @relation(fields: [businessId], references: [id], onDelete: Cascade)
  automation InstaOsAutomation   @relation(fields: [automationId], references: [id], onDelete: Cascade)
  Lead       Lead                @relation(fields: [leadId], references: [id], onDelete: Cascade)
  InstaOsDmOutbox InstaOsDmOutbox?

  @@index([businessId])
  @@index([businessId, createdAt])
  @@index([businessId, leadId])
  @@index([automationId])
  @@index([commentId])
  @@index([createdAt])
  @@index([leadId])
}

/// DM outbox — terminal states SENT/FAILED. PENDING = not yet sent via Graph.
enum InstaOsDmOutboxStatus {
  PENDING
  SENT
  FAILED
}

enum InstaOsEventStatus {
  PENDING
  /// Insta OS: DM is queued; Graph send is handled by the DM worker.
  DM_QUEUED
  SENT
  SKIPPED_DEDUP
  ROUTING_FAILED
  ERROR
}

enum InstaOsPostScope {
  ALL_POSTS
  SELECTED_POSTS
}

enum InstaOsNodeType {
  TRIGGER
  KEYWORD_ROUTER
  AI_CLASSIFIER
  // ACTION removed — reply variations now live inside DmTemplate.
  // Templates are the execution unit: they control both the DM body
  // and the comment reply variations. No separate action node needed.
}

enum BaseAgentType {
  STANDARD
  RECALL
  FLOW
}

model BaseAgent {
  id String @id @default(uuid())

  name   String
  description String? @db.Text

  prompt String @db.Text
  tools  Json   @default("[]")

  channel       CHANNEL @default(INSTAGRAM)
  baseAgentType BaseAgentType   @default(STANDARD)

  type    AGENT_TYPE @default(INTEGRATED)

  /// Published FLOW graph (mini-app chat agent). Draft lives in `currentFlow`.
  flow           Json?
  currentFlow    Json?
  flowSettings   Json?
  currentFlowSettings Json?

  /// When false, runtime skips this agent: webhooks/auto-connect/scheduled work treat it as paused.
  /// Mirrors the legacy `Agent.isActive` switch from the AI agent upsert form.
  isActive Boolean @default(true)
  agentMode AGENT_MODE @default(AUTO)

  /// When true, the agent gets booking tools from the MCP server (calendar booking).
  /// Standard agents: applies to the single-prompt turn. Flow agents: opt-in per objective node.
  bookingEnabled Boolean @default(false)

  /// When true, queue a CRMBridge patient writeback when a new lead is created on agent trigger.
  enableAutoPatientCreation Boolean @default(false)

  /// When true, the agent receives a `smart_stop` tool that can pause AI replies
  /// by switching the conversation to MANUAL when the model detects a configured
  /// escalation/stop condition (for example abuse or sensitive information).
  smartStopEnabled Boolean @default(false)
  smartStopDescription String? @db.Text
  smartStopStaticMessageEnabled Boolean @default(false)
  smartStopStaticMessage String? @db.Text

  /// Website embed widget config: { UI: { title, description }, chat: { primaryColor, title }, ... }
  widgetSettings Json?

  /// At most one auto-connect agent per scoped account when true; enforced in base-agent router.
  autoConnect Boolean @default(false)

  /// When set with autoConnect, this agent auto-attaches only for DMs on this Instagram account.
  instagramAccountId String?
  InstagramAccount   InstagramAccount? @relation("InstagramAccountBaseAgents", fields: [instagramAccountId], references: [id], onDelete: SetNull)

  /// Delivery surface for this base agent (GHL vs native channel APIs).
  provider PROVIDER @default(INSTAGRAM)

  // n8n workflow id
  n8nWorkflowId String?

  /// When set with autoConnect, scopes to this WhatsApp account. Null = all connected accounts.
  whatsAppAccountId String?
  WhatsAppAccount   WhatsAppAccount? @relation("WhatsAppAccountBaseAgents", fields: [whatsAppAccountId], references: [id], onDelete: SetNull)

  businessId String
  Business   Business @relation("BusinessBaseAgents", fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  recallPrimaryAgentId String?
  RecallPrimaryAgent   PrimaryAgent? @relation("PrimaryAgentBaseAgents", fields: [recallPrimaryAgentId], references: [id], onDelete: SetNull)

  ConversationsWithActiveAgent Conversation[] @relation("ConversationActiveBaseAgent")
  BaseAgentSessions             BaseAgentSession[]
  AgentTools                    AgentTool[]
  VoiceSessions                 VoiceSession[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([agencyId])
  @@index([recallPrimaryAgentId])
  @@index([businessId, autoConnect])
  @@index([instagramAccountId])
  @@index([whatsAppAccountId])
  @@index([businessId, channel, autoConnect])
}

/// Per-lead FLOW session: DB-backed `currentEvent` (ChatSession analogue for BaseAgent FLOW).
model BaseAgentSession {
  id String @id @default(uuid())

  conversationId String
  Conversation   Conversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)

  baseAgentId String
  BaseAgent   BaseAgent @relation(fields: [baseAgentId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead @relation(fields: [leadId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  /// Flow node id (matches JSON `nodes[].id`).
  currentEvent String?

  /// Optional runner scratchpad; canonical IG thread history is `ConversationMessage`.
  messages Json?

  flowEndedAt   DateTime?
  flowEndReason String?   @db.Text

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([conversationId, baseAgentId])
  @@index([leadId])
  @@index([businessId])
  @@index([baseAgentId])
  @@index([agencyId])
}

// ─────────────────────────────────────────────────────────────────────────────

model BusinessManager {
  id           String        @id @default(uuid())
  businessId   String
  Business     Business      @relation(fields: [businessId], references: [id], onDelete: Cascade)
  businessRole BUSINESS_ROLE @default(BUSINESS_OWNER)
  managerId    String
  Manager      User          @relation(fields: [managerId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([managerId])
}

model BusinessIntegration {
  id String @id @default(uuid())

  businessId String   @unique
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  // Twilio 
  twilioAccountSid String? @db.Text
  twilioAuthToken  String? @db.Text

  // Twilio Trunk - @todo must be a separate table (with multiple trunks)
  twilioTrunkAuthUsername      String? @db.Text
  twilioTrunkAuthPassword      String? @db.Text
  twilioTrunkTerminationSIPUrl String? @db.Text
  twilioTrunkSIPNumber         String?

  calenderIntegrations CalenderIntegration[]
  n8nIntegrations      N8NIntegration[]

  @@index([businessId])
}

model CalenderIntegration {
  id String @id @default(uuid())

  type CALENDAR_TYPE @default(CAL)

  calEventId  String
  calUsername String
  calApiKey   String

  /// Slack channel id for Cal booking notifications (requires Slack integration).
  slackNotificationChannelId   String?
  slackNotificationChannelName String?

  // Relations
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  businessIntegrationId String
  BusinessIntegration   BusinessIntegration @relation(fields: [businessIntegrationId], references: [id], onDelete: Cascade)

  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  PrimaryAgents PrimaryAgent[]

  @@index([businessId])
  @@index([businessIntegrationId])
}

model N8NIntegration {
  id String @id @default(uuid())

  name        String
  description String
  url         String
  token       String

  // Relations
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  businessIntegrationId String
  BusinessIntegration   BusinessIntegration @relation(fields: [businessIntegrationId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([businessIntegrationId])
}

enum RESTACK_SERVICE {
  PRIMARY
  SECONDARY
  TERTIARY
}

// Agentic Layer Tables
model ChatSession {
  id String @id @default(uuid())

  rsRunId   String // restack run id 
  rsAgentId String // restack agent id 

  status       AGENT_SESSION_STATUS @default(ACTIVE)
  statusReason String?              @db.Text // reason for the status change

  /// Which Restack deployment owns this run (standalone stacks; failover = new session + DB replay).
  restackService RESTACK_SERVICE @default(PRIMARY)
  /// Throttles attempts to move a SECONDARY session back to PRIMARY (see PRIMARY_FAILBACK_COOLDOWN_MS).
  lastPrimaryFailbackAttemptAt DateTime?

  messages     Json? // Message[]
  currentEvent String? // current event name

  // Relations
  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  primaryAgentId String
  PrimaryAgent   PrimaryAgent @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)

  agentId String
  Agent   Agent  @relation(fields: [agentId], references: [id], onDelete: Cascade)

  // sequence & followup 
  activeSequencePosition Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([leadId])
  @@index([agencyId])
  @@index([primaryAgentId])
  @@index([agentId])
  /// Inbound GHL/chat hot path: find ACTIVE session for lead + channel agent (process-inbound-ghl-message).
  @@index([leadId, agentId, status])
}

model VoiceSession {
  id String @id @default(uuid())

  rsRunId   String? // restack run id (set after agent starts)
  rsAgentId String? // restack agent id

  /// Browser/widget test sessions (no PSTN). Used for LiveKit join + token minting.
  isWebTest        Boolean @default(false)
  livekitRoomName  String?

  /// WhatsApp Business Calling API session id (wacid.*).
  whatsappCallId     String? @unique
  whatsAppAccountId  String?
  voiceChannel       CHANNEL?

  status       VOICE_SESSION_STATUS @default(CALLING)
  statusReason String?              @db.Text
  errorMessage String?              @db.Text

  // Relations
  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  primaryAgentId String
  PrimaryAgent   PrimaryAgent @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)

  agentId String?
  Agent   Agent?  @relation(fields: [agentId], references: [id], onDelete: Cascade)

  baseAgentId String?
  BaseAgent   BaseAgent? @relation(fields: [baseAgentId], references: [id], onDelete: Cascade)

  leadCallId String? 
  LeadCall   LeadCall? @relation(fields: [leadCallId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([leadId])
  @@index([agencyId])
  @@index([primaryAgentId])
  @@index([agentId])
  @@index([baseAgentId])
  @@index([status])
  @@index([leadCallId])
}

model ChatLog {
  id String @id @default(uuid())

  type    CHAT_LOG_TYPE @default(SUCCESS)
  logType LOG_TYPE      @default(EVENT)

  title       String
  description String? @db.Text // description of the log
  data        Json? // data of the log

  chatAgentId   String?
  chatAgentName String  @default("")

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  endReason     String  @default("")
  chatSessionId String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([leadId])
  @@index([chatAgentId])
  @@index([createdAt])
  @@index([leadId, createdAt])
}

model Automation {
  id String @id @default(uuid())

  name        String
  description String?           @db.Text
  status      AUTOMATION_STATUS @default(DRAFT)

  flow         Json? // react flow json 
  flowSettings Json? // react flow settings json 

  // Relations 
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  primaryAgentId String
  PrimaryAgent   PrimaryAgent @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)

  // Campaigns that have opted to run this automation alongside them.
  Campaigns Campaign[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([agencyId])
  @@index([primaryAgentId])
}

model AutomationSession {
  id String @id @default(uuid())

  rsRunId   String // restack run id 
  rsAgentId String // restack agent id 

  status       AGENT_SESSION_STATUS @default(ACTIVE)
  statusReason String?              @db.Text // reason for the status change

  primaryAgentId String
  PrimaryAgent   PrimaryAgent @relation(fields: [primaryAgentId], references: [id], onDelete: Cascade)

  leadId String
  Lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([leadId])
  @@index([primaryAgentId])
  @@index([rsAgentId])
  @@index([rsRunId])
}

model Provider {
  id       String   @id @default(uuid())
  provider PROVIDER

  isActive   Boolean @default(true)
  config     Json?
  businessId String

  Business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  PoolPhoneNumbers PoolPhoneNumber[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
}

// The log for voice call charged to business
model VoiceChargeLog {
  id String @id @default(uuid())

  totalCost  Float
  totalCalls Int

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([createdAt])
  @@index([businessId])
}

// The log for message cost charged to business
model MessageChargeLog {
  id String @id @default(uuid())

  totalCost  Float
  totalCalls Int

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([createdAt])
  @@index([businessId])
}

model CustomField {
  id         String             @id @default(uuid())
  key        String             @db.VarChar(255)
  label      String             @db.VarChar(255)
  type       String             @default("")
  values     CustomFieldValue[]
  ghlFieldId String?
  source     FIELD_SOURCE       @default(MANUAL)
  businessId String
  business   Business           @relation(fields: [businessId], references: [id], onDelete: Cascade)
  options    Json?
  createdAt  DateTime           @default(now())
  updatedAt  DateTime           @updatedAt

  @@unique([businessId, key])
  @@index([businessId])
}

model CustomFieldValue {
  id         String       @id @default(uuid())
  value      String?      @db.Text
  leadId     String
  fieldDefId String
  source     FIELD_SOURCE @default(MANUAL)

  lead        Lead        @relation(fields: [leadId], references: [id], onDelete: Cascade)
  customField CustomField @relation(fields: [fieldDefId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([leadId, fieldDefId])
  @@index([leadId])
  @@index([fieldDefId])
}

model SipTrunk {
  id       String             @id @default(uuid())
  provider SIP_TRUNK_PROVIDER @default(TWILIO)
  status   SIP_TRUNK_STATUS   @default(ACTIVE)

  config Json? // SIP trunk configuration 

  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([createdAt])
  @@index([updatedAt])
}

enum SIP_TRUNK_PROVIDER {
  TWILIO
}

enum SIP_TRUNK_STATUS {
  ACTIVE
  INACTIVE
}

enum AGENT_TYPE {
  INTEGRATED // platform managed agents
  N8N // n8n managed agents
  VAPI // vapi managed agents
  ELEVEN_LABS
  RETELL
  ULTRA_VOX
}

enum VOICE_AGENT_TYPE {
  CONVERSATION_PATHWAY
  SINGLE_PROMPT
}

enum CHAT_LOG_TYPE {
  SUCCESS
  ERROR
  INFO
  WARNING
}

enum LOG_TYPE {
  EVENT
  ACTION
  CHECK
  MESSAGE
  APPOINTMENT
}

enum AGENT_SESSION_STATUS {
  ACTIVE
  TERMINATED
  COMPLETED
  FAILED
}

enum VOICE_SESSION_STATUS {
  ACTIVE
  TERMINATED
  COMPLETED
  FAILED
  CALLING 
  QUEUED
}

enum AUTOMATION_STATUS {
  DRAFT
  ACTIVE
  PAUSED
  TERMINATED
  COMPLETED
  FAILED
}

enum ROLE {
  ADMIN
  AGENCY // agency 
  MANAGER // agency manager (Multiple business managers for an agency can be assigned to multiple businesses)
  SALES_AGENT // business sales agent
  BUSINESS_OWNER // business owner
}

enum ACCESS {
  USER
  ADMIN // manager have admin access to the agency (all businesses)
}

enum PLAN {
  FREE
  BUSINESS_BASIC
  BUSINESS_ADVANCE

  AGENCY_BASIC
  AGENCY_ADVANCE

  // Mini-app plans
  INSTA_OS_FREE
  INSTA_OS_PRO
}

enum MINI_APP_SUBSCRIPTION_PLAN {
  FREE
  PRO
}

enum MINI_APP_SOURCE {
  INSTA_OS
  REACTIVATION_OS
  DENTAL_BRIDGE
}

enum DENTAL_BRIDGE_BOOKING_MODE {
  DIRECT
  STRIPE_DEPOSIT
}

enum DENTAL_BRIDGE_PATIENT_STATUS {
  QUEUED
  IN_PROGRESS
  SUCCESS
  FAILED
}

enum DENTAL_BRIDGE_BOOKING_STATUS {
  PENDING_PAYMENT
  /// Slot saved locally; PMS patient writeback not complete yet.
  PENDING_PATIENT
  PAID
  QUEUED
  IN_PROGRESS
  SUCCESS
  FAILED
  /// Patient writeback failed while a local slot was held — staff action needed.
  PATIENT_PENDING_FAILED
  /// Patient ready but PMS appointment create failed — staff action needed.
  MANUAL_REQUIRED
  CANCELLED
}

enum PLAN_TERM {
  MONTHLY
  YEARLY
}

// Lead Enums 
enum LEAD_STATUS {
  NEW
  CONTACTED
  RETRYING
  JUNK
  BOOKED
}

enum LEAD_STATUS_TYPE {
  HOT
  WARM
  COLD
}

enum LEAD_QUALITY {
  UNQUALIFIED
  LOW
  MEDIUM
  HIGH
  PERFECT
}

enum LEAD_SOURCE {
  UNKNOWN
  INSTA_OS
  REACTIVATION_OS
  DENTAL_BRIDGE
  AIOS
  CAL
  WEBSITE_EMBED
  LANDING_PAGE
}

enum LANDING_PAGE_STATUS {
  DRAFT
  PUBLISHED
}

enum FUNNEL_FORM_FIELD_TYPE {
  TEXT
  EMAIL
  PHONE
  TEXTAREA
  NUMBER
}

enum FUNNEL_WEBHOOK_STATUS {
  PENDING
  SENT
  FAILED
  SKIPPED
}

/// Insta OS first-contact channel (use with leadSource = INSTA_OS). Set only on lead create.
enum LEAD_SUB_SOURCE {
  UNKNOWN
  INSTA_COMMENT
  INSTA_DM
  WHATSAPP_DM
  GM_SCRAPER
  CAL_BOOKING
  IMPORTED
}

// Meeting Enums

enum MEETING_STATUS {
  UPCOMING
  SUCCESS
  NO_SHOW
  CANCELLED
}

enum CALL_TYPE {
  INBOUND
  OUTBOUND
  WEB
}

enum CALL_END_REASON {
  customer_busy
  assistant_not_valid
  assistant_not_provided
  customer_ended_call
  customer_did_not_answer
  assistant_ended_call
  db_error
  exceeded_max_duration
  assistant_not_found
  assistant_said_end_call_phrase
  voicemail
  worker_shutdown
  unknown_error
}

// TRACK CALL PICKED/NOT_PICKED STAT -> Weather if call was picked by the user
enum CALL_STAT {
  PICKED
  NOT_PICKED
}

// TRACK USER REPLY STAT - Weather if user replied to the call
enum CALL_USER_REPLY_STAT {
  ANSWERED
  NO_ANSWER
}

// TRACK CALL RESULT -> SUCCESS/FAILURE -> Weather if call was successful or not
enum CALL_RESULT {
  SUCCESS
  FAILURE
}

enum CAMPAIGN_STATUS {
  DRAFT
  TESTING
  ACTIVE
  PAUSED
  COMPLETED
  FAILED
}

enum CAMPAIGN_TYPE {
  VOICE_REACTIVATION
  GMB_REVIEW
}

enum ReviewStatus {
  QUEUED
  REQUESTED
  RECEIVED
  DECLINED
  FAILED
}

enum RankingSource {
  GOOGLE_ORGANIC
}

enum BusinessMailProvider {
  GOOGLE
  OUTLOOK
}

enum CAMPAIGN_LEAD_STATUS {
  PENDING
  RETRYING
  IN_PROGRESS
  COMPLETED
  FAILED
}

enum CAMPAIGN_LEAD_QUALITY {
  LOW
  MEDIUM
  HIGH
}

enum VOICE_POOL_PROVIDER {
  VAPI
  RETELL
  ELEVEN_LABS
  LIVEKIT
  TWILIO
}

enum POOL_NUMBER_STATUS {
  ACTIVE
  WARMING
  PAUSED
  SUSPECTED
  BLACKLISTED
  REMOVED
}

enum CAMPAIGN_LEAD_RESULT {
  SUCCESS
  FAILURE
}

enum LEAD_CALL_TYPE {
  GLOBAL
  CAMPAIGN
}

enum CALL_SOURCE {
  RECALLSYNC
  VAPI
  RETELL
  ELEVENLABS
}

// Pipeline Enums
enum OPPORTUNITY_STATUS {
  OPEN
  LOST
  WON
  ABANDONED
}

// Automation Enums
enum EVENT {
  // PIPELINE EVENTS 
  OPPORTUNITY_EVENT
  STAGE_EVENT
  PIPELINE_EVENT

  // Opportunity events 
  OPPORTUNITY_CREATED
  OPPORTUNITY_UPDATED
  OPPORTUNITY_DELETED
  OPPORTUNITY_STATUS_CHANGED
  OPPORTUNITY_STAGE_CHANGED

  // Stage events
  STAGE_CREATED
  STAGE_UPDATED
  STAGE_DELETED

  // Pipeline events
  PIPELINE_CREATED
  PIPELINE_UPDATED
  PIPELINE_DELETED

  // Lead events
  LEAD_EVENTS
  LEAD_CREATED
  LEAD_UPDATED
  LEAD_DELETED
  LEAD_STATUS_CHANGED
  LEAD_QUALITY_CHANGED

  // Lead call events 
  LEAD_CALL_CREATED
  LEAD_CALL_UPDATED
  LEAD_CALL_DELETED

  // Meeting events 
  MEETING_EVENTS
  MEETING_CREATED
  MEETING_UPDATED
  MEETING_DELETED
  MEETING_STATUS_CHANGED

  //  CAMPAIGN EVENTS

  // Campaign lead events 
  CAMPAIGN_LEAD_CREATED
  CAMPAIGN_LEAD_UPDATED
  CAMPAIGN_LEAD_DELETED

  // Campaign events 
  CAMPAIGN_EVENTS
  CAMPAIGN_CREATED
  CAMPAIGN_UPDATED
  CAMPAIGN_DELETED
  CAMPAIGN_STATUS_CHANGED

  // Campaign call events 
  CAMPAIGN_CALL_CREATED
  CAMPAIGN_CALL_UPDATED
  CAMPAIGN_CALL_DELETED
  CAMPAIGN_CALL_STATUS_CHANGED

  // FOLLOWUP EVENTS
  FOLLOWUP_EVENTS
  FOLLOWUP_CREATED
  FOLLOWUP_UPDATED
  FOLLOWUP_DELETED
  FOLLOWUP_STATUS_CHANGED
  FOLLOWUP_PRIORITY_CHANGED

  // CONVERSATION AUTOMATIONS
  SEND_MESSAGE
}

enum PRIORITY {
  LOW
  MEDIUM
  HIGH
}

enum FOLLOW_UP_STATUS {
  PENDING
  COMPLETED
  RETRYING
  DROPPED
}

enum FOLLOW_UP_TYPE {
  AI_AGENT
  HUMAN_AGENT
}

enum FOLLOW_UP_SOURCE {
  MANUAL_SCHEDULED // created by lead
  AUTO_SCHEDULED // created by agent/us

  // Appointment follow ups
  UPCOMING_APPOINTMENT
  NO_SHOW_APPOINTMENT
  CANCELLED_APPOINTMENT

  ENGAGEMENT
}

enum PROVIDER {
  VAPI
  TWILIO
  GHL
  HUBSPOT
  N8N
  LLM
  ELEVEN_LABS
  CARTESIA
  RETELL
  ULTRA_VOX
  DEEPGRAM
  WHATSAPP
  INSTAGRAM
  SLACK
  CRM_BRIDGE
  DATAFORSEO
  UPLOADTHING
}

enum BusinessAssetType {
  IMAGE
  AUDIO
  VIDEO
  FILE
}

enum MESSAGE_ITEM_TYPE {
  MESSAGE
  CALL
}

enum SYSTEM_EVENT {
  AVAILABILITY_CHECK
  GET_APPOINTMENTS
  BOOK_APPOINTMENT
  CANCEL_APPOINTMENT
  RESCHEDULE_APPOINTMENT
  UPDATE_CONTACT
  AGENT_TRIGGERED
  AGENT_TERMINATED

  AUTOMATION_TRIGGERED
  AUTOMATION_TERMINATED
  AUTOMATION_MESSAGE_SENT
  CHAT_SESSION_RESTART

  /// Loop guard: too many inbound messages in a short window. The conversation
  /// is auto-switched to MANUAL reply mode to prevent agent-to-agent loops.
  AUTO_REPLY_GUARD_TRIPPED
}

enum MESSAGE_SOURCE {
  RECALLSYNC
  N8N
  GHL
}

enum SYSTEM_EVENT_STATUS {
  SUCCESS
  FAILED
  PENDING
  PARTIAL
}

enum SYSTEM_EVENT_TYPE {
  TOOL_CALL
  NOTIFICATION
  ACTION
  ERROR
}

enum MESSAGE_SENDER {
  AI_AGENT
  HUMAN_AGENT
  LEAD
  SYSTEM
}

enum CONVERSATION_MESSAGE_STATUS {
  SENT
  DRAFT
  SCHEDULED
  FAILED
  REJECTED
}

enum REPLY_MODE {
  AUTO
  MANUAL
}

enum AGENT_MODE {
  AUTO
  DRAFT
}

enum LEAD_NOTE_TYPE {
  NOTE // for notes
  NOTIFICATION // for notifications
  CONVERSATION // for conversations
}

enum MIGRATION_STATUS {
  PENDING
  IN_PROGRESS
  COMPLETED
  FAILED
}

// WhatsApp (DEFAULT) -> Standalone Channel (Every agent will have a SOURCE->DEFAULT->CHANNEL->WHATSAPP)
// GRM (GHL) -> Multiple Channels (Every Agent will have a SOURCE->GOHIGHLEVEL->CHANNEL->WHATSAPP)
enum SOURCE {
  DEFAULT // default source mean no source and only channel
  GOHIGHLEVEL // GHL is a source and will have multiple channels
  HUBSPOT
  ZOHO
  SALESFORCE
}

enum CHANNEL {
  EMAIL
  SMS
  WHATSAPP
  FACEBOOK
  INSTAGRAM
  LIVE_CHAT
  VOICE_CALL
  WP_VOICE_CALL
}

enum CALENDAR_TYPE {
  CAL
  CALENDLY
  GHL
}

enum MEETING_SOURCE {
  PLATFORM
  OUTSIDE
}

enum BUSINESS_ROLE {
  BUSINESS_OWNER
  BUSINESS_MANAGER
}

enum FIELD_SOURCE {
  MANUAL
  GHL
}

// Knowledgebase Model
model Knowledgebase {
  id          String  @id @default(uuid())
  name        String
  description String? @db.Text

  // Knowledgebase content stored as JSON
  // files: Array of {fileName: string, fileType: "pdf" | "txt", fileBody: string}
  files Json? @default("[]")

  // text: Single text content for free-form text input
  text String? @db.LongText

  // faqs: Array of {question: string, answer: string}
  faqs Json? @default("[]")

  // subKnowledgeBases: Array of {id: string, type: string, title: string, content: string}
  subKnowledgeBases Json? @default("[]")

  // Relations
  businessId String
  Business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  agencyId String
  Agency   User   @relation(fields: [agencyId], references: [id], onDelete: Cascade)

  // Many-to-many relation with PrimaryAgent
  PrimaryAgents PrimaryAgent[]

  // Metadata
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([businessId])
  @@index([agencyId])
}
